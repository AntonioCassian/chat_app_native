import { StyleSheet } from "react-native";

const styles =  StyleSheet.create({
    container: {
      padding: 20,
    },
    logo: {
      alignItems: 'center',
      marginBottom: 60,
      marginTop: 60,
    },
})

export default styles;